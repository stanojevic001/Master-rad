First, in order to run setup file and for the tool to work you need to install Python.
(preferably the latest stable version)

For Windows download installer from the link: 
https://www.python.org/downloads/

For Linux run the command:
sudo apt install python3