# left-hand term originates from ENABLE_LDCONFIG = ON/OFF at package build
if [ "ON" == "ON" ]; then
  echo -e "/opt/rocm/rocm_smi/lib\n/opt/rocm/rocm_smi/lib64" > /etc/ld.so.conf.d/x86_64-librocm_smi_lib.conf
  ldconfig
fi
